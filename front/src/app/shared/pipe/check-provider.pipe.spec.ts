import { CheckProviderPipe } from './check-provider.pipe';

describe('CheckProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
